---
- hosts : all
  become: true
  vars:
    ansible_user: ec2-user
  tasks:
    
    - name: clone docker-compose
      copy:
        src: ./docker-compose.yml
        dest: /home/ec2-user/
        owner: ec2-user
        group: ec2-user
        mode: "0644"
        directory_mode: "0755"

